{
  "name": "@luka598/orm",
  "private": false,
  "version": "0.0.0",
  "description": "Simple orm",
  "main": "main.js",
  "files": [
    "./**/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "tsc",
    "prepublish": "cp package.json dist && cp README.md dist && cp LICENSE dist && cp NOTICE dist",
    "pack": "cd dist && npm pack",
    "cleanup": "rm -rf dist/*",
    "ezpack": "npm run cleanup && npm run compile && npm run prepublish && npm run pack"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/luka598/orm.git"
  },
  "keywords": [
    "orm",
    "postgresql"
  ],
  "author": "luka598",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/luka598/orm/issues"
  },
  "homepage": "https://github.com/luka598/orm#readme",
  "devDependencies": {
    "typescript": "^4.7.4"
  }
}